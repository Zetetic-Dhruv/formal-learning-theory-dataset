# Core ML
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0
datasets>=2.18.0
accelerate>=0.28.0

# Serving
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Config
pyyaml>=6.0

# Optional: flash attention (requires CUDA)
# flash-attn>=2.5.0
